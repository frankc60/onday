{
  "name": "onday",
  "version": "2.4.6",
  "description": "Get some historical event on the given date (day / month)",
  "main": "onday.min.js",
  "author": "frankc60",
  "license": "ISC",
  "scripts": {
    "minifyJS": "terser ${npm_package_vars_source} --compress --mangle reserved=['onday','require','exports'],toplevel=true --warn --output onday.min.js",
    "production": "npm run version:bump --silent && rm -f onday.min.js && npm run minifyJS --silent && npm run version:add --silent",
    "version:add": "echo \"/**\\n * ${npm_package_name} v.$(npm run version:extract --silent)\\n * ©$(date '+%Y')\\n * ${npm_package_repository_url}\\n * Build time: $(date '+%d-%b-%Y %H:%M:%S')\\n */\\n$(cat ${npm_package_main})\" > ${npm_package_main}",
    "version:bump": "npm version patch --no-git-tag-version --silent",
    "version:extract": "cat package.json | grep version | head -1 | awk -F: '{ print $2 }' | sed 's/[\",]//g' | tr -d '[[:space:]]'"
  },
  "vars": {
    "source": "src/index.js",
    "comments": "echo \"aa ${npm_package_license}\""
  },
  "config": {
    "comments": "${npm_package_author}"
  },
  "devDependencies": {
    "terser": "^5.5.0"
  },
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/frankc60/onday"
  },
  "keywords": [
    "history",
    "promise",
    "dates"
  ]
}
