{
  "name": "onday",
  "version": "2.2.20",
  "description": "Get some historical event on the given date (day / month)",
  "main": "index.js",
  "scripts": {
    "minifyJS": "terser index.js --compress --mangle reserved=['onday','require','exports'],toplevel=true --warn --output onday.min.js",
    "production": "npm run version:bump --silent && rm -f onday.min.js && npm run minifyJS --silent && npm run version:add --silent",
    "version:add": "echo \"/**\\n * v.$(npm run version:extract --silent)\\n * ©$(date '+%Y') frankc60\\n * Build time: $(date '+%d-%b-%Y %H:%M:%S')\\n */\\n$(cat onday.min.js)\" > onday.min.js",
    "version:bump": "npm version patch --no-git-tag-version --silent",
    "version:extract": "cat package.json | grep version | head -1 | awk -F: '{ print $2 }' | sed 's/[\",]//g' | tr -d '[[:space:]]'"
  },
  "devDependencies": {},
  "dependencies": {
    "terser": "^5.5.0",
    "uglify-js": "^3.3.11"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/frankc60/onday"
  },
  "keywords": [
    "historical",
    "api",
    "retrieve"
  ],
  "license": "ISC",
  "author": "frankc60"
}
